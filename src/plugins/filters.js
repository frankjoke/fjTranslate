import Vue from "vue";
// import Vuex from "vuex";
/*
Vue.filter("capitalize", function capitalize(value) {
  if (!value) return "";
  value = value.toString();
  return value.charAt(0).toUpperCase() + value.slice(1);
});
Vue.filter("startcase", function startcase(value) {
  if (!value) return "";
  value = value.toString();
  return value
    .replace(/[_\-]+/g, " ")
    .replace(/([a-z])([A-Z])/g, (str, $1, $2) => $1 + " " + $2)
    .replace(/(\s|^)(\w)/g, (str, $1, $2) => $1 + $2.toUpperCase());
});
 
Vue.filter("truncate", function truncate(text, stop, clamp) {
  return text.slice(0, stop) + (stop < text.length ? clamp || "..." : "");
});

Vue.filter("ago", value => {
  return moment
    .utc(value)
    .local()
    .fromNow();
});
 */